import styled from 'styled-components';
import {pale} from '../../theme/variables';

export const Title = styled.h1`
    margin-top: 0;
    color: ${pale};
`;

export const P = styled.p`
    width: 98.4%;
`;